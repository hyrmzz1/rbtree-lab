#include "rbtree.h"

int main(int argc, char *argv[]) {
    // rbtree tree = *new_rbtree(); 이게 아니라
    rbtree *tree = new_rbtree();
    // rbtree_insert(tree, 10);
    //preOrder()
}
// 이 파일에서 테스트 돌려보자. src 디렉터리 빌드하면 이 파일도 나온다 !!